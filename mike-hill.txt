Link to finished codepen:https://codepen.io/mikehill345/pen/Yzqzzap

<!-- Questions to Submit in today's file first-lastname.txt --> 
<!--
Copy the questions below into the first-lastname.txt file on your github repo - answer each question and then push your changes. 

    1. What is Semantic HTML? it gives meaning to my website
    2. What is HTML used for? it is used for the structure for my website.
    3. What is an attribute and where do we put it? extra pieces of information you can tag we put it at the end of <div> or <section>
    4. What is the h1 tag used for? How many times should I use it on a page? the main title of the page it's the biggest and most important and should only be 1
    5. Name two tags that have required attributes <a> or <img>
    6. What do we put in the head of our HTML document? <header> or <nav>
    7. What is an id? an id is used to target a specific tag so you can link to it later
    8. What elements can I add an id to? any of them
    9. How many times can I use the same id on a page? 1 time
    10. What is a class? a selector 
    11. What elements can I add a class to? any elements
    12. How many times can I use the same class on a page? as many times as you want
    13. How do I get my link to open in a new tab?  add target="_blank" to your anchor tags
    14. What is the alt attribute in the image tag used for? if the image cannot be displayed it provides alternative information about the img
    15. How do I reference an id? #
    16. What is the difference between a section and a div section means the content inside is grouped div does not have any meaning any found in its class
    17. What is CSS used for? styling your website
    18. How to we select an element? Example - h1 
    19. What is the difference between a class and an id? - Give me an example of when I might use each one a class is for mutiple things an id is for one single thing when you call .batman it will call everything with the class batman if you call #batman the can only be one and thats all it will call
    20. How do we select classes in CSS? a . before the class name .batman{}
    21. How do we select a p element with a single class of “human””? .human 
    22. What is a parent child selector? When would this be useful?  it's used select the parent ie:class then narrow down to the child it works well in cases like mutiple lists
    23. How do you select all links within a div with the class of sidebar? .sidebar a{}
    24. What is a pseudo selector?is used to define a special state of an element for example it can be used to Style an element when a user mouses over it
    25. What do we use the change the spacing between lines? line-height:
    26. What do we use to change the spacing between letters? letter-spacing:
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform: uppercase, lowercase, capitalize.
    28. How do I add a 1px border around my div that is dotted and black? div { border: 1px dotted black;}
    29. How do I select everything on the page? *
    30. How do I write a comment in CSS? /* like this*/
    31. How do I find out what file I am in, when I am using the command line? it says it on the line above where you are typing
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this?  rm filename because there are no safegaurds when you delete something when you delete it it's gone forever
    34. Why should I use version control?  it's a great way for mutiple people to work on the same project and not interfere with each other and the work combines together 
    35. How often should I commit to github? every 20 mins or so or when you finish something signifigant 
    36. What is the command we would use to push our repo up to github? git push -u'yourname'
    37. Walk me through Lambda's git flow.  lambdas git flow is for the student to fork the project make a clone make a branch of your own then to do your project while commiting with comments of what was done then the end project gets commited and pushed back for turn in

Stretch Questions

    1. What is the difference between an inline element and a block element? block elements force a new line to begin where inline elements do not
    2. What happens when an element is positioned absolutely? An absolutely positioned element no longer exists in the normal document layout flow. Instead, it sits on its own layer separate from everything else
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
     display block by default- <address>,<ol>,<p>
    inline by default- <a>, <img>, 
    display inline- <span> 
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
the box model is being able to put every element of html into it's own box that can be altered or changed starting from a larger box to smaller boxes within it
2nd question: by setting the outer box the big <div> that wraps the whole thing the width


   --> 